name: Build

on:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:
    branches: [main]
  workflow_dispatch:

env:
  GO_VERSION: stable
  LIBOPENMPT_VERSION: 0.8.7

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  linux:
    name: linux-${{ matrix.goarch }}
    runs-on: ${{ matrix.runner }}
    env:
      CGO_ENABLED: 1
      GOOS: linux
    strategy:
      fail-fast: false
      matrix:
        include:
          - goarch: amd64
            runner: ubuntu-latest
            cc: clang
            cxx: clang++
          - goarch: arm64
            runner: ubuntu-24.04-arm
            cc: clang
            cxx: clang++
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true

      - name: Install system dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq clang llvm-dev pkg-config make libasound2-dev libopenmpt-dev

      - name: Build binary
        run: |
          set -eux
          export GOARCH=${{ matrix.goarch }}
          export CC="${{ matrix.cc }}"
          export CXX="${{ matrix.cxx }}"
          make build

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: neoviolet-linux-${{ matrix.goarch }}
          path: neoviolet
          if-no-files-found: error

  macos:
    name: macos-${{ matrix.goarch }}
    runs-on: ${{ matrix.runner }}
    env:
      CGO_ENABLED: 1
      GOOS: darwin
    strategy:
      fail-fast: false
      matrix:
        include:
          - goarch: amd64
            runner: macos-15-intel
            cc: clang
            cxx: clang++
          - goarch: arm64
            runner: macos-latest
            cc: clang
            cxx: clang++
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true

      - name: Install dependencies
        run: |
          brew update
          brew install pkg-config make libopenmpt

      - name: Build binary
        run: |
          set -eux
          export GOARCH=${{ matrix.goarch }}
          export CC="${{ matrix.cc }}"
          export CXX="${{ matrix.cxx }}"
          make build

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: neoviolet-macos-${{ matrix.goarch }}
          path: neoviolet
          if-no-files-found: error

  windows:
    name: windows-${{ matrix.goarch }}
    runs-on: ${{ matrix.runner }}
    env:
      CGO_ENABLED: 1
      GOOS: windows
    strategy:
      fail-fast: false
      matrix:
        include:
          - goarch: amd64
            runner: windows-latest
            msystem: MINGW64
            env_prefix: mingw64
            pkg_prefix: mingw-w64-x86_64
            compiler_pkg: mingw-w64-x86_64-gcc
            libopenmpt_pkg: mingw-w64-x86_64-libopenmpt
            go_pkg: mingw-w64-x86_64-go
            cc: gcc
            cxx: g++

          - goarch: arm64
            runner: windows-11-arm
            msystem: CLANGARM64
            env_prefix: clangarm64
            pkg_prefix: mingw-w64-clang-aarch64
            compiler_pkg: mingw-w64-clang-aarch64-clang
            libopenmpt_pkg: mingw-w64-clang-aarch64-libopenmpt
            go_pkg: mingw-w64-clang-aarch64-go
            cc: clang
            cxx: clang++

    defaults:
      run:
        shell: msys2 {0}

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup MSYS2
        uses: msys2/setup-msys2@v2
        with:
          msystem: ${{ matrix.msystem }}
          update: true
          install: >-
            ${{ matrix.compiler_pkg }}
            ${{ matrix.pkg_prefix }}-pkgconf
            ${{ matrix.libopenmpt_pkg }}
            ${{ matrix.pkg_prefix }}-libogg
            ${{ matrix.pkg_prefix }}-libvorbis
            ${{ matrix.pkg_prefix }}-zlib
            ${{ matrix.go_pkg }}
            make
            curl
            ca-certificates
          path-type: inherit

      - name: Build binary
        run: |
          set -eux
          # Verify Go installation
          go version

          export GOARCH=${{ matrix.goarch }}
          export CC="${{ matrix.cc }}"
          export CXX="${{ matrix.cxx }}"
          export CGO_CFLAGS="$(pkg-config --cflags libopenmpt)"
          export CGO_LDFLAGS="$(pkg-config --static --libs libopenmpt)"

          make build

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: neoviolet-windows-${{ matrix.goarch }}
          path: neoviolet.exe
          if-no-files-found: error